export interface HasManyOptions {
  foreignKey: string;
  as: string;
}

export interface HasOneOptions {
  foreignKey: string;
  as: string;
}

export interface BelongsToOptions {
  foreignKey: string;
  as: string;
}
